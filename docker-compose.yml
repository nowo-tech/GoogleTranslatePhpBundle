name: google-translate-php-bundle
services:
  php:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: google-translate-php-bundle-php
    working_dir: /app
    volumes:
      - .:/app
      - composer-cache:/root/.composer/cache
      - coverage-data:/app/coverage
    environment:
      - COMPOSER_ALLOW_SUPERUSER=1
    command: ["tail", "-f", "/dev/null"]
    networks:
      - google-translate-php-bundle-network

volumes:
  composer-cache:
    driver: local
  coverage-data:
    driver: local

networks:
  google-translate-php-bundle-network:
    driver: bridge
